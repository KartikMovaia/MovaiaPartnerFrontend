# Leave blank to use the same-origin Vite dev proxy (recommended — keeps the
# httpOnly auth cookie first-party). Set a full URL (e.g. https://api.example.com/api)
# only for a deliberate cross-origin deployment, which then needs SameSite=None cookies.
VITE_API_URL=
